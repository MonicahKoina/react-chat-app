specVersion: "v1"
serviceId: "react-chat-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "react-chat-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
